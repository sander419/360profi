# Файлы, которые исполняются на Linux-сервере: только LF.
# С CRLF systemd не запустит юнит, а bash не найдёт интерпретатор в шебанге.
*.sh text eol=lf
*.service text eol=lf
*.timer text eol=lf
*.conf text eol=lf
